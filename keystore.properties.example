# Copy to keystore.properties and point it at your own keystore.
# Without this file assembleRelease still builds, just unsigned.
storeFile=tharaa-release.jks
storePassword=your-store-password
keyAlias=tharaa
keyPassword=your-key-password

